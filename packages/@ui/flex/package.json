{
  "name": "@huteming/ui-flex",
  "version": "1.2.2-alpha.1",
  "description": "Flex 是 CSS flex 布局的一个封装。",
  "main": "dist/ui-flex.common.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vue-cli-service build --target lib --name ui-flex group.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vue/cli-service": "^3.4.0"
  }
}
