import PickerAddress from './picker-address'
import { installComponent } from 'packages/ui/src/utils/tools'

PickerAddress.install = installComponent('PickerAddress', PickerAddress)

export default PickerAddress
