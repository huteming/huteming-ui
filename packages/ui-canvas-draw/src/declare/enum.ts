export enum ImageTypes {
    PNG = 'png',
    JPG = 'jpg',
}

export enum LineTypes {
    FillText = 'fillText',
    StrokeText = 'strokeText',
}

export enum ArcTypes {
    Fill = 'fill',
    Stroke = 'stroke',
    Close = 'closePath',
}
